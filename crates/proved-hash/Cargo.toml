[package]
name = "proved-hash"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
thiserror.workspace = true
hex.workspace = true
serde = { workspace = true }
zeroize.workspace = true
winter-crypto = { workspace = true }
winter-math = { workspace = true }

[dev-dependencies]
proptest.workspace = true
serde_json = "1"

[lints.rust]
# UN seul `unsafe` dans ce crate (effacement mémoire au drop, digest.rs),
# autorisé nommément par `#[allow(unsafe_code)]`. `deny` (et non `forbid`) rend
# cette exception unique possible ; toute AUTRE occasion échouera la compilation.
unsafe_code = "deny"
